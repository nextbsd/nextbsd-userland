# ~/.zprofile -- run once by login shells (the console, ssh, Terminal).
#
# Environment that every program you start should see belongs here, for
# example:
#
#   export EDITOR=vi
#   path+=(~/bin)
#
# Interactive settings (prompt, aliases) belong in ~/.zshrc instead.
