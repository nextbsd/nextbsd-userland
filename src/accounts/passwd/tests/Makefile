# Host-side tests for passwd(1). No root, nothing real touched.
#
#   make check
#
# The interactive change path is not covered here and cannot be: see the note
# at the top of the script.

check:
	sh passwd_test.sh

clean:
	@:

.PHONY: check clean
