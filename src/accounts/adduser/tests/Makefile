# Host-side tests for adduser(8). Builds a test binary pointed at a fixture
# directory and runs the suite; needs no root and touches nothing real.
#
#   make check
#
# The suite runs under sh deliberately; see the note at the top of the script.

check:
	sh adduser_test.sh

clean:
	@:

.PHONY: check clean
